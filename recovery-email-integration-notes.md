# إعداد تنبيهات استرداد الحسابات

- بريد مالك المتجر المستهدف لاستلام طلبات الاسترداد: `concordsharaf@gmail.com`.
- المستخدم وافق على استخدام قناة Gmail مجانية لا تطلب من مستخدمي المتجر بريدًا إلكترونيًا.
- جرى تسجيل الدخول إلى حساب Google المالك وفتح إنشاء مشروع Google Apps Script لإعداد نقطة استقبال ترسل رسالة إلى المالك عند طلب الاسترداد برقم الهاتف.
- المسار المرغوب: يرسل المستخدم رقم هاتفه فقط، ثم يتصل به المالك للتحقق قبل أن يعيد تعيين رمز مؤقت من إدارة الحسابات.
- أُلغي مفتاح طوارئ الأدمن نهائيًا بطلب من المستخدم. تعتمد شاشة الدخول الآن طلبًا برقم الجوال فقط، ولا تعيد أي رمز تلقائيًا.

## مراجع قناة البريد المجانية

يمكن أن يستقبل تطبيق ويب Google Apps Script طلبات `POST` عبر `doPost(e)` وينفذ بسلطة مالك النص البرمجي عند اختياره لهذا الإعداد، ثم يرسل بريدًا باستخدام `MailApp.sendEmail()`. توضح وثائق Google أن حساب Gmail الاستهلاكي يملك حصة تصل إلى 100 مستلم بريد يوميًا، وهو نطاق ملائم لطلبات استرداد نادرة. بقي إنشاء مشروع Apps Script من المتصفح بطيئًا في هذه الجلسة بعد تسجيل الدخول، لذلك يُستخدم FormSubmit كمسار مؤقت مباشر لإرسال الطلب إلى بريد المالك؛ يجب تفعيل البريد مرة واحدة قبل قبول الطلبات الفعلية.

المراجع: https://developers.google.com/apps-script/guides/web ، https://developers.google.com/apps-script/reference/mail/mail-app ، https://developers.google.com/apps-script/guides/services/quotas ، https://formsubmit.co/documentation.

## التفعيل والتحقق

فُعّل نموذج FormSubmit لبريد مالك المتجر، واستخدم التطبيق المعرّف غير الكاشف للبريد `fc46f51ed31eb26af7d65edd8a313358`. أُرسل طلب تجريبي برقم وهمي `000000000` ووصل إلى بريد المالك بعنوان «اختبار وصول طلب استرداد حسابي». لا يرسل التطبيق رمزًا بديلًا في البريد؛ البريد يحتوي رقم الجوال ومرجع الطلب فقط، ويعيد المالك ضبط رمز مؤقت يدويًا بعد التحقق الهاتفي.
