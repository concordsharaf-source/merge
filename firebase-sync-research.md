# ملاحظات بحث مزامنة Firebase

## مصادر رسمية

1. [Firestore: العمل دون اتصال](https://firebase.google.com/docs/firestore/manage-data/enable-offline)
2. [Firestore: قواعد الحماية](https://firebase.google.com/docs/firestore/security/get-started)
3. [Firestore: المعاملات والكتابات المجمعة](https://firebase.google.com/docs/firestore/manage-data/transactions)

## نتائج التصميم المهمة

- يدعم Cloud Firestore التخزين الدائم دون اتصال في الويب، لكنه يحتاج إلى تفعيله صراحة. يعيد العميل مزامنة الكتابات المحلية تلقائيًا عند عودة الاتصال، بينما تكون قاعدة التعارض الافتراضية «آخر كتابة تفوز» للعمليات المتزامنة على المستند نفسه.
- تحتاج بيانات المتجر إلى Firebase Authentication وقواعد Firestore مرتبطة بالمستخدم/المتجر؛ قاعدة تسمح للجميع بالقراءة والكتابة غير مناسبة للإنتاج.
- معاملات Firestore وعمليات الكتابة المجمعة ذرية، لكن المعاملات لا تنجح أثناء انقطاع الإنترنت. لذا يجب أن تبقى IndexedDB المحلية مصدر التنفيذ الفوري دون اتصال، ثم تُرسل أحداث محاسبية غير قابلة للتعديل عند عودة الشبكة.
- للتطبيق المحاسبي، التصميم المقترح هو: متجر مستقل بمعرّف ثابت، سجل أحداث مرقّم لكل فاتورة/حركة مخزون/دفعة/مرتجع، نسخ قراءة محلية، ومعاملة سحابية للتحقق من إصدار المخزون أو الاعتراض على التعارض بدل استبدال كامل قاعدة البيانات.
- يجب ألا ترحّل البيانات القائمة تلقائيًا أو تحذفها قبل إنشاء نسخة احتياطية JSON وتأكيد المستخدم لاختيار المتجر المقصود.

## حالة الإعداد

- أُنشئ مشروع Firebase مستقل باسم `Hesabi Backup` على خطة Spark.
- سُجل تطبيق ويب باسم `Hesabi Backup Web` من دون تفعيل Firebase Hosting، لأن الاستضافة الحالية على Vercel تبقى كما هي.
- لا يستخدم المشروع Firebase Storage، ولم تُفعّل أي خدمة مدفوعة أو وسيلة دفع. فُعل Firebase Authentication بالبريد/كلمة المرور وCloud Firestore فقط.

## حدود النسخ المجاني المعتمدة

- لا يمكن استخدام Cloud Storage في مشروع `Hesabi Backup` على خطة Spark الحالية؛ صفحة الخدمة تطلب ترقية المشروع. لذلك لن يُستخدم Cloud Storage أو أي خدمة تتطلب وسيلة دفع.
- يوفر Firestore مجانًا قاعدة واحدة لكل مشروع، وسعة 1 GiB، و50,000 قراءة و20,000 كتابة و20,000 حذف يوميًا. الحد الأقصى للمستند الواحد 1 MiB، والحد الأقصى لطلب الكتابة 10 MiB.
- يضغط التطبيق JSON في المتصفح عند دعم `CompressionStream`، مع رجوع آمن إلى بيانات غير مضغوطة. ثم يقسم النص المرمز Base64 إلى أجزاء حدها التشغيلي 512 KiB داخل مجموعة فرعية خاصة بالنسخة؛ وهذا أقل بكثير من حد المستند 1 MiB. تُحفظ بيانات الفهرس منفصلة، ولا يسمح التطبيق بأكثر من ثلاث نسخ مكتملة لكل حساب نسخ.
- تستعيد العملية نسخة وقائية محلية أولًا، ثم تتحقق من جميع الأجزاء والبصمة قبل الاستبدال. إذا تجاوزت النسخة الحد التشغيلي أو تعذر رفعها، يبقى التصدير المحلي JSON متاحًا ولا تُحذف البيانات المحلية.

## نتيجة إعداد Firestore

- أُنشئت قاعدة Firestore الافتراضية بنجاح في مشروع `Hesabi Backup` عبر Google Cloud Console.
- الإعدادات المختارة هي **Standard Edition** و**Native mode** مع قواعد **Restrictive**، والموقع متعدد المناطق `nam5` الذي عرضته اللوحة بوصفه مؤهلًا للحصة المجانية.
- لم يُفعّل خيار Start free ولم تُربط وسيلة دفع أو ترقية مدفوعة. القاعدة فارغة حاليًا، والخطوة التالية هي إعداد تسجيل دخول مجاني وقواعد وصول لا تسمح إلا لصاحب النسخة.

## إعدادات الحماية وتطبيق الويب

- فُعل Firebase Authentication بنجاح مع موفر **Email/Password** المجاني فقط، ولم يُفعّل الهاتف أو ميزات Identity Platform التي تطلب ترقية.
- أضيف نطاقا التطبيق المنشوران إلى Authorized domains: `hesab-tau.vercel.app` و`hesabipwa-2r9mmdzn.manus.space`، إضافةً إلى نطاقات Firebase الافتراضية.
- إعدادات تطبيق الويب العامة تشير إلى مشروع `hesabi-backup` ومجال المصادقة `hesabi-backup.firebaseapp.com` ومعرّف التطبيق `1:1060015017841:web:b19a9d31e79cf0476c30d0`. لا تُعامل Firebase Web API key على أنها سر؛ إذ تكون مرئية في تطبيقات الويب، بينما تمنع قواعد Firestore والمصادقة الوصول غير المصرح به.

## التنفيذ الفعلي في حسابي

> النسخ السحابي هنا **نسخ يدوي** وليس مزامنة فورية. يستمر IndexedDB المصدر الوحيد للعمل دون اتصال، ولا تنتقل أي عملية إلى جهاز آخر إلا عندما يختار الأدمن «إنشاء نسخة سحابية» ثم «استعادة» صراحة.

| المكوّن | ما نُفذ |
|---|---|
| هوية النسخ | حساب Firebase Email/Password مستقل عن حسابات الأدمن والكاشير المحلية، مع `browserLocalPersistence`. |
| العزل | `backupOwners/{uid}/backups/{backupId}` و`chunks/{chunkId}`؛ قواعد `firestore.rules` المنشورة تسمح لصاحب `uid` وحده بالقراءة والكتابة. |
| سلامة البيانات | فهرس يحفظ عدد الأجزاء، الحجم، الترميز، إصدار قاعدة البيانات، ملخص المخازن، وبصمة SHA-256 للبايتات الدقيقة. |
| الاستعادة | تنزيل نسخة JSON وقائية محلية أولًا، ثم تنزيل كل الأجزاء وترتيبها والتحقق من العدد والحجم والبصمة وفك الترميز والتحقق البنيوي قبل مسح IndexedDB واستعادته. |
| الجلسات | لا تتضمن نسخة IndexedDB علامة جلسة التطبيق؛ بعد أي استعادة ينتقل المستخدم إلى شاشة الدخول المحلية. جلسة Firebase المنفصلة لا تستبدل حساب الأدمن. |
| الاحتفاظ | تبقى آخر ثلاث نسخ مكتملة فقط للحساب؛ يحذف التطبيق الأقدم بعد نجاح الرفع، ولا يحذف بيانات IndexedDB تلقائيًا. |

الحد التشغيلي المحلي للبايتات المضغوطة أو الخام قبل Base64 هو 12 MiB، وهو حارس استخدام محافظ لتجنب رفع نسخ كبيرة على خطة Spark. عند تجاوز الحد أو انقطاع الشبكة لا يغير التطبيق البيانات المحلية، ويبقى تصدير JSON المحلي متاحًا.

## نتيجة التحقق الحالية

نجح اختبار إعداد Firebase الخفيف في الوصول إلى Firebase Authentication دون إنشاء مستخدم أو بيانات. ونجحت اختبارات وحدة مستقلة لتجزئة النسخة، وإعادة بنائها، ورفض جزء معدّل بسبب فشل البصمة، ومنع تجاوز حد الحجم. كما تأكدت واجهة إعدادات الأدمن وحوار الربط بصريًا، ولم يظهر خطأ في وحدة تحكم المتصفح. لا يتضمن هذا التحقق إنشاء حساب Firebase تجريبي أو رفع بيانات متجر فعلية؛ يلزم إجراء هذه الخطوة بحساب نسخ يختاره مالك المتجر قبل وسم الاختبار التكاملي الخارجي كمكتمل.
